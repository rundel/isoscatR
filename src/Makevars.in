## Use the R_HOME indirection to support installations of multiple R version
PKG_CPPFLAGS = @CPPFLAGS@
PKG_LIBS = @LDFLAGS@ @LIBS@ $(shell $(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()" ) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
